# ============================================================
# Analytical-Force — Dependências para deploy online (HF Spaces / Docker)
# ------------------------------------------------------------
# Conjunto LEVE: API + pipeline + IA via HF Inference Providers (modelo
# hospedado, rápido). NÃO inclui torch/transformers — o Space não roda o
# modelo localmente; ele chama a inferência hospedada (MODEL_PROVIDER=hf_inference).
#
# Para rodar o modelo LOCALMENTE no Space (MODEL_PROVIDER=transformers), seria
# preciso adicionar torch/transformers/accelerate — porém é lento em CPU.
# ============================================================

# --- API HTTP ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- Núcleo do agente ---
simple-salesforce>=1.12.5
pandas>=2.2.0
python-dotenv>=1.0.1
libsql>=0.1.0
requests>=2.31.0
pydantic>=2.6.0
python-dateutil>=2.9.0

# --- Relatório ---
markdown>=3.6

# --- IA hospedada (MODEL_PROVIDER=hf_inference) ---
huggingface_hub>=0.26.0
